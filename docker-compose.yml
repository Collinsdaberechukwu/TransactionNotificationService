version: '3.8'

services:
#  db:
#    image: mysql:8.0
#    container_name: mysql_container
#    restart: unless-stopped
#    environment:
#      MYSQL_ROOT_PASSWORD: collins1234
#      MYSQL_DATABASE: TransactionNotificationDb
#      MYSQL_PASSWORD: collins1234
#    volumes:
#      - database_data:/mysql-dataz:/var/lib/mysql
#    ports:
#      - "3309:3306"
#
#    healthcheck:
#      test: [ "CMD", "mysqladmin", "ping", "-h", "127.0.0.1" ]
#      interval: 30s
#      timeout: 30s
#      retries: 5
#      start_period: 40s


  backend:
    build: .
    container_name: backend-container
    restart: always
    ports:
      - "16221:9000"
    environment:
      SPRING_DATASOURCE_URL: ${DB_URL}
      SPRING_DATASOURCE_USERNAME: ${DB_USERNAME}
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}


